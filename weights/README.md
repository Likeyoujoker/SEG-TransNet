# Weights Placeholder

No pretrained or trained weights are included in this repository.

- For the fine-tuning path retained in `options/train_seg.json`, place the official ×4 initialization checkpoint at `weights/x4.pth`.
- For evaluation, place your trained model at the path passed to `test.py --model_path`.
